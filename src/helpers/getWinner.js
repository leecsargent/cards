// export default state => {
//   // console.log(Object.keys(state));
//   console.log(state)
//   const ready = Object.keys(state).filter(item => !!state[item].next.length).length === 1;
//   return null;
// };
