export default ({ dealer, player}) => {
  // const dealerStands = dealer.stand;
  // console.log('dealerStands', dealerStands);
  console.log('checking for winner', { dealer, player});

  return {
    winner: 'dealer'
  }
}
